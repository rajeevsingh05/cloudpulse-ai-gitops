namespace: dev
frontend:
  replicaCount: 1
  image:
    tag: dev-19
backend:
  replicaCount: 1
  image:
    tag: dev-19
aiService:
  replicaCount: 1
  image:
    tag: dev-19
config:
  environment: dev
secret:
  appSecret: dev-secret-placeholder
hpa:
  backend:
    minReplicas: 1
    maxReplicas: 3
  aiService:
    minReplicas: 1
    maxReplicas: 3
ingress:
  host: ""
keyVault:
  secretName: cloudpulse-dev-app-secret
